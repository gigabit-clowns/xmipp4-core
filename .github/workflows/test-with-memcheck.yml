name: Run tests with memcheck
permissions:
  contents: read

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  test_with_memchecks:
    name: Test with memchecks
    env:
      BUILD_TYPE: Debug
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -el {0}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install memcheck tool
        uses: ./.github/composites/install-memtool
        id: install-memtool

      - name: Configure and build with CMake
        uses: threeal/cmake-action@v2
        with:
          source-dir: ${{ github.workspace }}
          build-dir: "${{ github.workspace }}/build"
          c-compiler: gcc
          cxx-compiler: g++
          options: |
            CMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }}
            CMAKE_EXPORT_COMPILE_COMMANDS=ON
          run-build: true
          build-args: --config ${{ env.BUILD_TYPE }}

      - name: Run tests with memory check
        uses: ./.github/composites/run-ctest
        id: run-ctest
        with:
          check-memleaks: true

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Show memory leak logs (if exist)
        if: steps.run-ctest.outputs.exist-memleaks == 'true'
        uses: ./.github/composites/show-memleaks
        with:
          test-numbers: ${{ steps.run-ctest.outputs.memleak-test-numbers }}
