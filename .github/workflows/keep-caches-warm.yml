name: Keep caches warm

permissions: {}

on:
  schedule:
    - cron: '37 5 * * *'

  pull_request:
    paths:
      - '.github/workflows/keep-caches-warm.yml'

  workflow_dispatch:

concurrency:
  group: keep-caches-warm-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sweep:
    uses: gigabit-clowns/.github/.github/workflows/keep-caches-warm.yml@main
    with:
      entries: >-
        [
          {"os": "ubuntu-latest", "prefix": "ccache-ubuntu-latest-g++"},
          {"os": "ubuntu-latest", "prefix": "ccache-ubuntu-latest-clang++"},
          {"os": "ubuntu-22.04", "prefix": "ccache-ubuntu-22.04-g++"},
          {"os": "ubuntu-22.04", "prefix": "ccache-ubuntu-22.04-clang++"},
          {"os": "macos-latest", "prefix": "ccache-macos-latest-clang++"},
          {"os": "windows-latest", "prefix": "ccache-windows-latest-cl"},
          {"os": "windows-latest", "prefix": "ccache-windows-latest-g++"},
          {"os": "windows-11-arm", "prefix": "ccache-windows-11-arm-cl"},
          {"os": "windows-11-arm", "prefix": "ccache-windows-11-arm-g++"},
          {"os": "ubuntu-latest", "prefix": "fetched-Linux", "path": "/tmp/fetched"},
          {"os": "macos-latest", "prefix": "fetched-macOS", "path": "/tmp/fetched"},
          {"os": "windows-latest", "prefix": "fetched-Windows", "path": "C:/fetched"}
        ]
